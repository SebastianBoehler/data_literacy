how many lines per stop

get bus trip and see delays over trip

do all coordinates share the same base weather station

check if stops are parsed properly

instead of next 10 stops,
get next busses within the next 1h
